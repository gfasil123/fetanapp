// Firebase Storage Rules - Allow all authenticated users to read and write
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to any authenticated user
      allow read, write: if request.auth != null;
    }
  }
} 